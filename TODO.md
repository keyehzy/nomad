# TODO / out of scope

The following are intentionally **not** implemented yet. They are out of scope
for the current design and recorded here as potential future work:

- Standalone `qoal` parser (the Python DSL is the source frontend for now).
- Bosonic CCR (commutator) algebra.
- Full point-group irreducible representations.
- HFB/Bogoliubov approximation passes.
- QIR emission.
- Native hardware routing.
